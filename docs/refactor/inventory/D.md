# Bundle D — data files

Doc and Data Inventory (Pass 3). Read-only survey. Writer/reader scripts and line numbers
are taken from `docs/PIPELINE.md`'s artifact table (line ~92-112, authoritative, dated
2026-09-22) and per-call sections, cross-checked with `grep` for anything not in that
table. Category legend: live input (human/external-authored) / live output tracked
(real decisions, not a mechanical rebuild) / regenerable cache (gitignored, rebuild
command exists) / human view (gitignored, read by nothing, exists for eyeballing) /
one-shot artifact (produced once, not part of the live rebuild chain) / orphan (no
reader) / variant experiment (an alternate calibration, superseded).

## Repo-root files

| File | Tracked?/gitignored? | Written by | Read by | Size | Category | Recommended action | Reason |
|---|---|---|---|---|---|---|---|
| `Dictionary.pickle` | untracked, gitignored | `Dictionary.readCorpus`/`__main__` in `dictionary.py` (`:653`) | every exporter and diagnostic script (`util/_theoryio.py`, `util/build_*`, `scratch/*regret*.py`) via `sys.modules["__main__"].Dictionary` alias trick | 56M | regenerable cache | keep, gitignore (already) | Core cache; correctly gitignored, correctly not checked against source freshness (tracked as bug **B15** in `todo.md`). |
| `FirstTheory.pickle` | untracked, gitignored | `Dictionary.buildTheory` (`dictionary.py:855`) | same population as above plus `util/export_plover_dictionary.py` (legacy path, superseded by `loadFinalTheory`) | 50M | regenerable cache | keep, gitignore (already) | Theory 1 cache; feeds theory 2 via `util/_theoryio.py`. |
| `ambiguity_report.tsv` | untracked, gitignored | `src/ambiguitychecker.py:1394` (`__main__`) | none found (`grep` shows only the writer and `SHARED_DISCRIMINATOR_REWIRE_PLAN.md`/`scratch/reform1990/STATUS.md` prose) | 2.8M | human view / diagnostic output | keep, gitignore (already) | Human-eyeball diagnostic from `ambiguitychecker.py`'s own `__main__`, not part of the live rebuild chain (`docs/PIPELINE.md` doesn't list it in the artifact table). No code reader — an orphan by design (diagnostic-only), not a bug. |
| `conjugation_disambiguation_order.txt` | **tracked** | human-authored (repo root, per `CLAUDE.md`/memory `conjugation_disambiguation_spec`) | `util/check_conjugation_disambiguation_order.py` (`docs/PIPELINE.md` §S6.Elicitation.6); linked, not copied, from `docs/specs/discriminating-features.md` §2.3 | 4.0K | live input / spec | keep + already documented | Authoritative human-authored precedence spec — correctly tracked, correctly linked not duplicated. |
| `conjugation_disambiguation_report.json` | untracked, gitignored | `util/check_conjugation_disambiguation_order.py` (`docs/PIPELINE.md:1013`) | none found beyond its own writer/doc mentions | 4.0K | regenerable cache / report | keep, gitignore (already) | Validator output, regenerate on demand. |
| `elicitation_answers.json` | **tracked** | a person, through the questionnaire web page (`docs/PIPELINE.md:103`) | `src/elicitation.py` (`resolveGroupPressSets` etc.), `util/build_pers3_default_answers.py`, `util/check_conjugation_disambiguation_order.py` | 68K | live input | keep + document in ARCHITECTURE | Primary human-authored data — the single most load-bearing tracked JSON in the repo besides the lexicon TSVs. |
| `elicitation_answers_pers1default.json` | **tracked** | `util/build_pers3_default_answers.py`'s `transform()` (one-off, run once) | only itself and `util/build_pers3_default_answers.py` (as a documented-but-inert input path) | 64K | variant experiment (archived) | **flag: orphan?** — keep as archive or delete | This is the *original* answer set (model 1, pers_1-default), archived after model 2 (pers_3-default, i.e. today's `elicitation_answers.json`) was adopted per `RESUME_2026-09-19-cpsat.md`. No live code path reads it for the pipeline; it exists only for historical comparison (`pers_1PreferedOver_pers_3KeyAssignation`, also archival). Candidate for `scratch/` or deletion — ask whether the comparison is still wanted. |
| `elicitation_questionnaire.html` | untracked, gitignored | `util/build_questionnaire_page.py` (`docs/PIPELINE.md:997`) | published as a Claude Artifact (external, not read by repo code) | 64K | regenerable / one-shot artifact | keep, gitignore (already) | Regenerated from `questionnaire.json`; the *published* copy (external Artifact URL) is the one actually used by the person answering — this file is just the last local render. |
| `elicitation_questionnaire_pers3default.html` | untracked, gitignored | one-off script run mentioned in `RESUME_2026-09-19-phaseG.md`/`-cpsat.md` (no current `__main__` entry point for it) | none | 56K | orphan / one-shot artifact (stale) | delete or regenerate-on-demand | Companion to the now-archived pers1-default comparison; `RESUME_2026-09-19-cpsat.md` itself calls the underlying `resolved_press_sets_pers3default.json` "a stale leftover from the pre-adoption exploration." Same status here. |
| `excluded_words.txt` | **tracked** | human-edited (word-exclusion list, e.g. bitter family per `RESUME_2026-09-19.md`) | `dictionary.py:102-103` (`Dictionary.readCorpus`), `src/nomAdjParadigm.py:381` (`EXCLUDED_WORDS_PATH_DEFAULT`) | 4.0K | live input | keep + already documented (`CLAUDE.md`, `docs/GLOSSARY.md`, `docs/PIPELINE.md`) | Correctly tracked, correctly documented, actively read by two modules. |
| `feature_keypress_feasibility.tsv` | untracked, gitignored | `src/ambiguitychecker.py:1420` (`__main__`, diagnostic) | none found as a reader | 8.0K | diagnostic output (superseded) | delete or gitignore-only | `RESUME_2026-09-18.md` flags the file's *predecessor* (`anchor_feasibility.tsv`, apparently renamed since) as "stale... regenerate or delete; don't trust its numbers" — this is the diagnostic path that fed the now-superseded solver-picks-features design. No reader in the live pipeline. |
| `keypress_groups.json` | **tracked** | `src/featuregroupingsat.py:530` (`serializeAssignment`), run via `util/build_keypress_groups.py` | `dictionary.py`, `src/ambiguitychecker.py`, every exporter, `util/build_realization_report.py`, `util/export_practice_words.py`, `scratch/*regret*.py` | 4.0K | live output (tracked) | keep + already documented | The Grouping Phase's canonical decided output — correctly tracked (real decisions, not mechanical). |
| `pers3default_repair_log.txt` | **tracked** | `util/build_pers3_default_answers.py`'s `repairConflicts()` (one-off) | none found | 4.0K | one-shot artifact | archive with `elicitation_answers_pers1default.json` or delete | Provenance note for the now-archived model-1-vs-2 comparison; only meaningful alongside `elicitation_answers_pers1default.json`. |
| `plover_stenalgo_dictionary.json` | **tracked** | `util/export_plover_dictionary.py:35` (`main`, `docs/PIPELINE.md:1588`) | loaded directly into the real Plover application (external), not read by repo code | 4.9M | live output (tracked) | keep + already documented | Real deliverable, correctly tracked (same convention as `starboard3h.json`/`realization_report.json` per `RESUME_2026-09-21-steno-trainer.md`). |
| `questionnaire.json` | untracked, gitignored | `src/elicitation.py` `buildQuestionnaireItems` (`docs/PIPELINE.md:985`) | `util/build_questionnaire_page.py`, `util/build_keypress_groups.py`, `src/featuregrouping.py` | 48K | regenerable cache | keep, gitignore (already) | Regenerated from the pickles; feeds the questionnaire page and Grouping Phase's live-marker check. |
| `realization_report.json` | **tracked** | `util/build_realization_report.py:38` (`docs/PIPELINE.md:1288`, "report build" path) | `util/export_keyboard_layout.py` (trainer keyboard legend only) | 60K | live output (tracked) | keep + already documented; watch **B18** | The "report build" call site — can drift from the "inline path" (`Dictionary.buildFinalTheory`) if not rebuilt together; tracked as **B18** in `todo.md`. Already flagged live, no new finding. |
| `requirements.txt` | **tracked** | human-edited | `pip install -r requirements.txt` (`CLAUDE.md`) | 4.0K | live input | keep | Standard, no issue. |
| `resolved_press_sets.json` | untracked, gitignored | `src/elicitation.py:471` (`serializeResolvedPressSets`, `docs/PIPELINE.md:1084`) | `dictionary.py`, `src/ambiguitychecker.py`, `util/build_keypress_groups.py`, `util/build_realization_report.py`, every exporter, `scratch/*regret*.py` | 31M | regenerable cache | keep, gitignore (already) | Large but correctly gitignored/regenerable; central intermediate artifact. |
| `resolved_press_sets_pers3default.json` | untracked, gitignored | one-off inline script (`RESUME_2026-09-19-phaseG.md`'s "How to regenerate model 2's derived files" snippet — no `__main__` entry point) | none in live code | 12M | variant experiment (stale/orphan) | delete | Its own generating session's notes call it "now a stale, meaningless filename" once model 2 became the *primary* `elicitation_answers.json`/`resolved_press_sets.json` — this file's name no longer describes what it holds relative to the adopted default. Safe to delete; regenerable in principle if ever needed again, but nothing currently points at it. |
| `starboard3h.json` | **tracked** | Keyboard Layout Optimization (S4), last run before commit `37fdc4e` (per `docs/PIPELINE.md:99`); no live command regenerates it (`dictionary.py:494`'s solver call is commented out, per `CLAUDE.md`/`todo.md`) | `dictionary.py`, nearly every module and exporter, `plover_stenalgo/plover_stenalgo/system.py`/`_generated_keys.py` | 4.0K | live output (tracked, frozen) | keep + already documented | Central, correctly tracked; the "no regeneration command" gap is already tracked in `todo.md`'s "Queued follow-ups." |
| `theory.tsv` | untracked, gitignored | `dictionary.py:847` (human view of theory 1) | none (per `CLAUDE.md`: "Nothing reads theory2.tsv"; `theory.tsv` likewise has no reader, only `theory2.tsv` is explicitly called out) | 2.5M | human view | keep, gitignore (already) | Eyeball-only artifact, written only on a `FirstTheory.pickle` miss (tracked as **B14**, "goes stale with the caches"). |
| `theory2.tsv` | untracked, gitignored | `dictionary.py` (`docs/PIPELINE.md:1506`) | **none** — `CLAUDE.md` states explicitly "Nothing reads `theory2.tsv`" | 6.7M | human view (orphan by design) | keep, gitignore (already) | Explicitly documented orphan — not a bug, a deliberate human-eyeball artifact; every real exporter recomputes theory 2 through `util/_theoryio.py` instead. |

## Directories

| Dir | Tracked?/gitignored? | Written by | Read by | Size | Category | Recommended action | Reason |
|---|---|---|---|---|---|---|---|
| `images/` | **tracked** (`images/Stenalgo.png`, 1 file) | human-added | `README.md` | 676K | live input (doc asset) | keep | Ordinary README image, no issue. |
| `morphalou/` | untracked, gitignored | external download (Morphalou 3.1 corpus, not repo-generated) | `util/crossCheckNomAdjWithMorphalou.py`, `src/nomAdjParadigm.py`, `util/validateLexiconAgainstNomAdjParadigms.py`, `util/generateMissingNomAdjForms.py` | 670M | live input (external reference corpus) | keep, gitignore (already) | Large external cross-check corpus for Synthetic Lexicon Building (S2) verb-paradigm tooling; correctly excluded from git (670M). Not documented in `docs/PIPELINE.md`'s artifact table or `README.md` setup instructions — **a fresh clone has no pointer to where to obtain this file**, worth one line in `README.md` or `docs/RECOMPUTE.md` ("download Morphalou 3.1 CSV to `morphalou/5/Morphalou3.1_CSV.csv` before running the NOM/ADJ cross-checkers"). |
| `pers_1PreferedOver_pers_3KeyAssignation` (file, no extension, repo root) | **tracked** | human-authored finding note (2026-09-19, the pers_1-vs-pers_3 model comparison) | none (referenced only by RESUME files, Bundle C) | 8.0K, 97 lines | history / archived finding | archive with the two pers1default files or delete | Companion to `elicitation_answers_pers1default.json` — same archival status. Odd filename (no extension, reads like a sentence) worth renaming if kept. |

## `scratch/` (untracked unless noted; `scratch/callgraph` excluded per task scope)

| File | Tracked? | Written by | Read by | Size | Category | Recommended action | Reason |
|---|---|---|---|---|---|---|---|
| `scratch/combined_regret.py` | untracked | human, ad hoc | run manually (`python scratch/combined_regret.py`) | 16K | one-shot diagnostic | move to `scratch/` (already there) or delete | Reproduces the headline 10x star/hash regret number from `RESUME_2026-09-20-starhash-priority.md`; that number is now in `docs/specs/star-hash-marking.md` §2. Script itself still runs against live pickles if ever needed to re-verify — low cost to keep, no cost to delete (regenerable from the spec's description). |
| `scratch/combined_regret_30x.py` | untracked | human, ad hoc | manual | 16K | one-shot diagnostic (sensitivity variant) | delete | Only used once for the 10x/30x/100x threshold comparison table, now fully captured in `docs/specs/star-hash-marking.md` §2. |
| `scratch/combined_regret_100x.py` | untracked | human, ad hoc | manual | 16K | one-shot diagnostic (sensitivity variant) | delete | Same as above. |
| `scratch/cross_category_regret.py` | untracked | human, ad hoc | manual | 8.0K | one-shot diagnostic | delete | Bucket-2-alone raw analysis, superseded by the pooled `combined_regret.py` and the shipped `decideStarHashMark`. |
| `scratch/cross_lemma_regret.py` | untracked | human, ad hoc | manual | 12K | one-shot diagnostic | delete | Bucket-3-alone raw analysis, same status. |
| `scratch/lemma_pair_exemption.py` | untracked | human, ad hoc | manual | 8.0K | one-shot diagnostic | delete | Distinct-lemma-pair rollup (1,325→1,085), one-off input to the Rule-3 doublet investigation; result now encoded in `loadReform1990DoubletPairs` (shipped code). |
| `scratch/sameLemmeHomophoneResolution.txt` | untracked | human, unexplained ("a one-line stray note, never explained" per `RESUME_2026-09-19-cpsat.md`) | none | 4.0K | orphan | **ask the user**, then delete or fold in | Flagged as mysterious in two separate RESUME files, never resolved. Worth one direct question before deleting. |
| `scratch/callgraph` (dir) | untracked | pasted session transcript (per multiple RESUME files) | none | — | orphan (explicitly out of this bundle's scope per the task) | excluded from this table by task instruction | Listed only for completeness; task explicitly excludes it from Bundle D. |
| `scratch/reform1990/RESUME_2026-09-16.md`, `STATUS.md` | tracked | — | — | — | — | see Bundle C | Covered fully in `docs/refactor/inventory/C.md`, not repeated here. |
| `scratch/reform1990/annexe.wikitext` | tracked | fetched from Wiktionnaire (external) | `scratch/reform1990/match_reform_pairs.py` (research-only) | 36K | one-shot artifact (research input) | move to archive or delete with the rest of `reform1990/` | Raw scrape input for the now-complete `resources/reform1990.tsv` sourcing work. |
| `scratch/reform1990/subpages.json` | tracked | fetched from Wiktionnaire MediaWiki API (external) | `scratch/reform1990/match_reform_pairs.py` | 216K | one-shot artifact (research input) | same as above | Same status — raw scrape data, superseded by the finished `resources/reform1990.tsv`. |
| `scratch/reform1990/match_reform_pairs.py` | tracked | human-authored research script | manual (`python3 scratch/reform1990/match_reform_pairs.py`) | 12K | one-shot / research tool | same as above | The edit-distance heuristic matcher that helped build `reform1990.tsv`; not part of any live rebuild chain. |
| `scratch/reform1990/cat3_extract.py`, `cat5_6_matches.tsv`, `cat7_matches.tsv` | tracked | same research thread, per-category extraction/output | manual/none | 8.0K / 4.0K / 4.0K | one-shot artifact (research input/output) | same as above | Category-specific intermediate research outputs, all superseded by the finished, sourced `resources/reform1990.tsv`. |

**Whole-directory note**: `scratch/reform1990/` (9 files, all git-tracked, ~280K total excluding the two `.md` covered in Bundle C) is a complete, self-contained research archive for one now-finished task (building `resources/reform1990.tsv`). Its own `STATUS.md` documents its history exhaustively; the durable output (`resources/reform1990.tsv`) lives outside it, in `resources/`. Recommend one decision for the whole directory rather than per-file: archive (e.g. move under a `docs/refactor/archive/` or similar, or tag a git commit and delete) vs. delete outright vs. leave as-is since it's small and harmless.

## Questions for triage

1. `elicitation_answers_pers1default.json` + `pers3default_repair_log.txt` + `pers_1PreferedOver_pers_3KeyAssignation`: archive together (they're one comparison study) or delete? None are read by live code.
2. `elicitation_questionnaire_pers3default.html` + `resolved_press_sets_pers3default.json`: delete now (both gitignored, both explicitly called "stale" in their own generating session's notes) — any reason to keep?
3. `scratch/reform1990/` (9 tracked files, ~280K): archive as a unit, or delete outright now that `resources/reform1990.tsv` is the durable output?
4. `scratch/sameLemmeHomophoneResolution.txt`: ask the user directly what it was for before deleting — flagged as unexplained in two separate session notes already.
5. `morphalou/` (670M, gitignored, external Morphalou 3.1 download): add a one-line download pointer to `README.md`/`docs/RECOMPUTE.md` so a fresh clone can reproduce Synthetic Lexicon Building (S2)'s cross-checkers?
6. `ambiguity_report.tsv` and `feature_keypress_feasibility.tsv`: both diagnostic-only outputs of `src/ambiguitychecker.py`'s `__main__` with no readers — keep as ad hoc diagnostics (gitignored, regenerate on demand) or note them in `docs/ARCHITECTURE.md`'s artifact table for discoverability?
7. `scratch/*regret*.py` (5 files, all superseded by the shipped `decideStarHashMark`/`loadReform1990DoubletPairs` and by `docs/specs/star-hash-marking.md`'s documented numbers): delete now, or keep as regenerable proof scripts in case the 0.5%-gap headline number is ever challenged?
8. Should `docs/ARCHITECTURE.md`'s planned artifact table include the gitignored intermediate caches (`Dictionary.pickle`, `resolved_press_sets.json`, etc.) at all, or only the tracked, human-meaningful outputs (`keypress_groups.json`, `realization_report.json`, `plover_stenalgo_dictionary.json`, `starboard3h.json`)? Affects how much of this table's "keep + document" recommendations actually need writing up.
